\name{get.slice.network}
\alias{get.slice.network}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{ creates a network object that appropriately represents the dynamic network for the specified time period}
\description{
  'Slices' a \code{\link{dynamicnetwork}} acording to the specified time parameter. Copies edges and attributes that land within the time range.
}
\usage{
get.slice.network(dynamnet, time.point)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{dynamnet}{  \code{dynamnet} an object of class \code{\link{dynamicnetwork}} containing data describing changing edges and attributes}
  \item{time.point}{  \code{time.point} value currently an integer, an interval in the future  specifying the time point where the dynamic network should be sliced.  }
}
\details{
  The network returned is a new object (so this may be slow) and changes made to it will not modify the original dynamic network.  
}
\value{
  An new object of class \code{network}
}

\author{ Skye Bender-deMoll \email{skyebend@skyeome.net}, CSDE statnet team }

\seealso{  }
\examples{

         }
\keyword{ graphs }% at least one, from doc/KEYWORDS
