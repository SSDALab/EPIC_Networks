\name{get.edges.spellmatrix}
\alias{get.edges.spellmatrix}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{ get the edge timing information in spellmatrix form }
\description{
  returns the edge timing information in the form of a two column matrix in which
  each row gives the start and end time for the corresponding edge id.
}
\usage{
get.edges.spellmatrix(net)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{net}{ a dynamic network }
}

\value{
  a two column matrix of valid intervals
}
\references{ ~put references to the literature/web site here ~ }
\author{ skyebend@skyeome.net }

\seealso{  \code{\link{get.spells.for}} }
\examples{
dyn <- as.dynamic(network.initialize(5));
dyn <- add.edge.dynamic(dyn,0,1,2);
dyn <- add.edge.dynamic(dyn,1,1,3);
dyn <- add.edge.dynamic(dyn,2,1,4);
dyn <- add.edge.dynamic(dyn,3,1,5);
get.edges.spellmatrix(dyn);
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ graphs }

